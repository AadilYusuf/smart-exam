# import streamlit as st

# score = 0
# for q in questions["mcq"]:
#     answer = st.radio(q["question"], q["options"])
#     if answer == q["correct"]:
#         score += 1

# st.write(f"Your Score: {score}/{len(questions['mcq'])}")
